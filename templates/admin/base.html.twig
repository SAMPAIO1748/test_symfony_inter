<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Document</title>
	</head>
	<body>

		<header>
			<h1>TEST SYMFONY INTERMEDIAIRE - Partie administrateur</h1>
			<h2>{{ admin }}</h2>
			{% for product in gproducts.getAll() %}
				<h3>{{product.name}}</h3>
			{% endfor %}
			<form action="{{ path('admin_search') }}" method="get">
				<label for="term">Recherche</label>
				<input type="search" name="term" id="term">
				<input type="submit" value="Rechercher">
			</form>
		</header>

		<main>
			{% block main %}{% endblock %}
		</main>


	</body>
</html>
