{% extends "front/base.html.twig" %}

{% block main %}

	{{form_start(userForm)}}

	{{form_label(userForm.email, 'Votre email')}}
	{{ form_widget(userForm.email) }}

	{{ form_label(userForm.name, 'Votre nom') }}
	{{form_widget(userForm.name)}}

	{{form_label(userForm.firstname, 'Votre prénom')}}
	{{form_widget(userForm.firstname)}}

	{{form_label(userForm.password, 'Votre mot de passe')}}
	{{form_widget(userForm.password)}}

	{{form_widget(userForm.Enregistrer)}}

	{{form_end(userForm)}}

{% endblock %}
